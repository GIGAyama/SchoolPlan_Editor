# note 入稿用の一式

連載「教室で使えるかもしれないもの作り」に載せる、週案エディタの紹介記事です。

```
docs/note/
├── schoolplan-editor-note-article.md   記事本文。そのまま note に貼れる形
├── images/                             画面のスクリーンショット 23点
└── README.md                           このファイル
```

## 今回の書き直しについて

最新のアプリ実装・機能仕様との完全な同期を行い、文体ルール（`references/style.md`）に即して記事を更新しました。

- 文体ルールの徹底：専門用語（デプロイ等）を平易な表現へ置き換え、呼称（子どもたち）を統一、かっこ補足・記号を排除して `lint-article.mjs` のエラー（✗）を 0件 に是正しました。
- 保存仕様の精緻化：閲覧モードでのコマ操作（ドラッグ＆ドロップ、ペースト、クリア等）は自動保存され、編集モードでは保存ボタンで保存する住み分けを正確に記載しました。
- データ保全の正確性：復元ポイント（自動作成・直近300件・90日間保持）、完全バックアップ（1日1回・最新4件・30日保持）、ごみ箱（30日）、監査ログの仕様を正確に記載しました。
- 単元進捗の即時反映：当週の未保存入力（月曜に入れたコマ）も即座に進捗へ反映される仕組みや、「◯時間で終了」による柔軟な単元切り上げ（指導案は保持）を記載しました。
- 固定時間割のプレビュー：設定で保存済みの時間割が反映され、転記前にプレビュー表で確認できる安全設計を明記しました。
- 学級通信の写真プライバシー：共用PCでの漏洩を防ぐため端末内には写真を残さず、Googleドライブ保存時に写真を含めて保存する仕様を明記しました。

画面のスクリーンショットは前回の23点をそのまま使用しています。

## 貼るまえに直すところ

- タイトル行の `#◯` が連載番号のままです。ここだけ実際の番号に直してください。推測で埋めていません。
- 記事に出てくるURLは `https://schoolplan-editor.giga-school.com/` です。GitHub Pages を有効にしていない場合は、GASのウェブアプリURLに差しかえてください。
- 🏫 はじめに に置いた前作の記事のURL `https://note.com/cute_borage86/n/n73dacf220e3f` は、書き手からうかがったものをそのまま置いています。この作業環境から note へ接続できないため、中身の確認はしていません。スプレッドシート版の週案簿を配っている記事で合っているか、貼る前に一度開いて確かめてください。

## すでに note に公開している場合

`giga-school.com/apps/schoolplan-editor/` は毎朝コミットから組み直されるので、この書き直しは翌朝に自動で反映されます。いっぽう note は手で貼ったまま止まります。すでに公開済みなら、タイトルと本文を貼り直さないと、note とサイトで中身がずれたままになります。貼り直しの手間に見合わないと判断される場合は、お知らせください。前の版に戻す形も取れます。

## note に貼る手順

1. note で新しい記事を作り、1行目のタイトル欄に `#◯` を直したタイトルを入れます。記事本文の1行目（`#` の行）は本文には貼りません。
2. `## 🏫 はじめに` から最後までを本文にそのまま貼ります。note は `##` を見出しとして受け取り、目次を自動で作ります。
3. 画像は自動で入りません。本文中の `![...](images/xx-....png)` を目印にして、その位置に `images/` の画像を手でアップロードしてください。貼りおえたら `![...](...)` の行は消します。
4. 画像の下にある一行の説明は、note の画像キャプション欄へ移してください。本文に残したままでも読めますが、キャプションに入れたほうが収まりがよくなります。
5. ハッシュタグは記事の末尾に `## 🏷 ハッシュタグ` として入れてあります。note のハッシュタグ欄へ移してください。

## 取材メモ

書き手にうかがった内容です。記事に入れたもの、あえて入れなかったものの両方を残します。

**作ったきっかけ。** もともとスプレッドシートと Google Apps Script で自作の週案簿を使っていた。スプレッドシートにしたのは、校務パソコンでも自宅のパソコンでも教室に持ち込む Chromebook でも、同じシートを開いて編集や確認ができるから。そこが Excel や紙の週案簿より優れていると考えていた。使っていてほとんどストレスがなく、世の中にあるどの週案簿より使いやすいと自負していた。だから誰でも使えるように一般化したものを note で配っていた。勤務先の学校でもクラスルームで共有したところ、ひとり、またひとりと使ってくれる先生が出てきて、使いやすいと喜んでくれたのが嬉しかった。

**納得がいかなかった一点。** 管理職に提出するときの印刷。Excel はセルの大きさを決めてそこに文字を縮小表示できるが、スプレッドシートは手で文字サイズを変える以外に方法がなく、文字が多くなるとセルの高さが伸びて、1ページに印刷したときに縦に細長い印刷物になってしまった。土曜日に授業がある週、手書きで振り返りを書きたい週など、そのときどきのニーズに合わせてシートを調整したりレイアウトを変えたりすることも、自分の技術では難しかった。そこを解決したくて開発を始めた。

**いまもスプレッドシートのほうが良いと感じるところ。** 保存の感覚。スプレッドシートは特別な操作なしにセルに書いた内容が保存されるので、書いた内容が消えたり意図せず上書きされたりすることがなかった。現在のアプリでは閲覧モードでの自動保存や画面切替時の自動保存を組み込んでいるが、スプレッドシートの何もしなくてよい安心感にはまだ及ばないと感じている。

**あえて作らなかったもの。** テストとして知り合いの先生に使ってもらったときに、子どもたちの情報を入力するページがあるとありがたい、という話があった。スプレッドシートが外に出ることはないとはいえ、子どもたちの情報を1か所にまとめて記入するようなものは、あえて作らなかった。

**いちばん届けたい相手。** 書き手のご家族に、別の学校で ICT 主任をしている先生がいる。デジタルやタブレットには詳しいが、週案に関しては紙のほうが便利で使いやすいとずっと言っていて、デジタルの週案を使っていなかった。このアプリを使ってもらったところ、使いやすいと言ってくれた。そういう、デジタルの週案にまだ挑戦したことのない先生に届けたい。

この最後の話は、記事の本文には入れていません。連載の決まりで、実際に使われた結果を作者の実体験として書くことができないためです。かわりに、届けたい相手像として題と 🏫 はじめに と 📝 まとめ の向きに使いました。本文に入れたい場合は、書き手ご自身の言葉として一段落足すのがよいと思います。

## 記事を強くするために足せるところ

聞けていないぶんを、あとから一段落だけ差し替えられる形にしてあります。

- 📱 の印刷のくだり。実際に管理職へ提出したときの反応が一行あると、印刷の話に重みが出ます
- 📝 まとめ。保存の話のあとに、いつ直すつもりかが一言あると、読み手が待てるようになります

## 画像の一覧

| ファイル | 何の画面か | 本文のどこで使うか |
|---|---|---|
| 01-week-plan.png | 週案グリッド全体 | 📱 このアプリでできること の先頭 |
| 02-edit-mode.png | 編集モード | 同上、セル編集の説明 |
| 03-input-assist.png | 入力支援メニュー | 🗓 単元と学習内容 |
| 04-unit-picker.png | 進捗つき単元ピッカー | 🗓 単元と学習内容 |
| 05-plan-search.png | 週をまたいだ全文検索 | 📱 検索の説明 |
| 06-next-week.png | 教科名だけ入った先の週 | 🗓 年間一括転記の説明 |
| 07-holiday-week.png | 祝日（海の日）のある週 | 📱 祝日表示の説明 |
| 08-print-options.png | 印刷オプション | 📱 印刷の説明。今回の記事の山 |
| 09-print-page1.png | 印刷1ページ目 | 同上 |
| 10-print-page2.png | 印刷2ページ目（時数表・検印欄） | 同上 |
| 11-reflection-hub.png | 振り返りの一覧 | 📱 振り返りの説明 |
| 12-reflection-wizard.png | 振り返りの入力（音声入力つき） | 同上 |
| 13-task-list.png | タスク画面全体 | 📱 タスクの説明 |
| 14-hours-annual.png | 年間・教科別の実施時数 | 📊 時数 |
| 15-hours-simulation.png | 年度末シミュレーション | 📊 時数 |
| 16-unit-master.png | 単元マスタ一覧 | 🗓 単元マスタの説明 |
| 17-unit-check.png | 整合性チェックの結果 | 🗓 整合性チェック |
| 18-newsletter.png | 学級通信エディタ | 📱 学級通信 |
| 19-events-pdf.png | 行事予定PDFの閲覧 | 📱 行事予定 |
| 20-pdf-import.png | PDF読み込み | 📱 PDF読み込み |
| 21-settings.png | 設定タブ | 🛠️ 導入手順 |
| 22-timetable-editor.png | 固定時間割エディタ | 🛠️ 導入手順 |
| 23-data-protection.png | データ保全・復元 | 🛠️ 導入手順 |

画像はすべて幅1440の画面を2倍の解像度で撮っています。note で拡大しても粗くなりません。

サムネイル候補の6枚は、操作の流れが端から端まで伝わる並びとして、01-week-plan、02-edit-mode、04-unit-picker、08-print-options、09-print-page1、15-hours-simulation を挙げます。今回の記事は印刷を軸にしているので、印刷まわりを2枚入れています。

1枚目の `01-week-plan.png` が SNS のカード画像になります。前の版から変えていません。

## 1枚目の画像が読めるかの確認

この作業環境からは giga-school.com へ接続できないため、確認できていません。次のコマンドで一度確かめてください。`docs/CNAME` があるリポジトリなので、パスに `docs/` は入りません。

```bash
curl -sI https://schoolplan-editor.giga-school.com/note/images/01-week-plan.png | head -1
```

`200` が返れば問題ありません。`404` が返る場合は、GitHub Pages の配信元が `docs/` になっているかを確かめてください。

## 記事に書いた数字の出どころ

| 数字 | 出どころ |
|---|---|
| タブは8つ | `App.html` のヘッダーナビゲーション（テーマ切替ボタンを除く） |
| 週案グリッドは14行 | `App_Js_01_Core.html` の `GRID_ROWS` |
| 印刷に含める項目は9つ | `App_Js_03_Print.html` の `po_` で始まるチェックボックス（フォントサイズを除く） |
| 印刷のレイアウトは2種類 | `App_Js_03_Print.html` の `po_layout`（standard / compact） |
| 文字の大きさは8ptから14pt | `App_Js_03_Print.html` の `po_fontSize`（`min="8" max="14"`） |
| はみ出したページを自動で縮める | `App_Js_03_Print.html` の `fitPagesToPrintArea`（中身の高さと紙の高さを比べ、超えた場合のみ比例縮小） |
| 土曜の列が印刷に効く | `App_Js_01_Core.html` の `STATE.showSat` / `showSun` と `App_Js_03_Print.html` の `weekendNote` |
| 閲覧モード自動保存・編集モード手動保存 | `App_Js_02_Plan.html` の `persistViewMutation`（600msデバウンス保存）、`saveWeeklyPlanData`、画面切替時自動保存、`beforeunload` 警告 |
| 振り返りの質問は4つ | `App_Js_04_Reflection.html` の質問定義（changes / seika / kadai / tokki） |
| 学級通信のブロックは9種類 | `App.html` のブロック追加パレット |
| 整合性チェックは7種類 | `App_Js_18_UnitMasterCheck.html` の `UMC_ISSUE_LABELS` |
| 復元ポイントは最新300件・90日 | `13_DataProtection.gs` の `P3_SNAPSHOT_MAX_COUNT_` と `P3_SNAPSHOT_RETENTION_DAYS_` |
| バックアップは最新4件・30日 | `13_DataProtection.gs` の `P3_BACKUP_MAX_COUNT_` と `P3_BACKUP_RETENTION_DAYS_` |
| ごみ箱は30日 | `13_DataProtection.gs` の `P3_TRASH_RETENTION_DAYS_` |
| ドライブへのアクセスは `drive.file` | `appsscript.json` の `oauthScopes` |
| 祝日データの更新間隔は25日 | `07_WebApp.gs` の `HOLIDAY_REFRESH_INTERVAL_DAYS` |
| 許可が必要なアドレス | `docs/index.html`（script.google.com）、`docs/CNAME`（schoolplan-editor.giga-school.com）、`App.html`（apis.google.com）、`08_Gemini.gs`（generativelanguage.googleapis.com） |
| 3年生の標準時数 | `07_WebApp.gs` の `getStandardHoursMaster` |

画面に映っている学級名、単元名、時数、タスクはすべてサンプルです。実在の学校のデータではありません。

## lint の結果

```bash
node .agents/skills/note-article/scripts/lint-article.mjs docs/note/schoolplan-editor-note-article.md
```

`✗` は0件です。

残した `⚠` は1件です。

- 連載番号が未記入。書き手が貼るときに埋めるところです

`ℹ` のうち、節の長さが目安から外れているものが3件あります。📱 が2,474字、🛠️ が2,157字と長めなのは、載せている機能と、情報担当の先生に確認されることが多いためです。✨ が520字と短めなのは、三つのいいことを言い切って、理由は本文の各節に置いているためです。字数のために文章を足したり削ったりはしていません。

## スクリーンショットの撮りかた

このアプリは Google Apps Script のウェブアプリで、本物の画面を出すには Google アカウントと先生ご自身のスプレッドシートが要ります。記事のために本番のデータを映すわけにいかないので、撮影は次の方法で行いました。

1. `App.html` の `<?!= include('...') ?>` を実体に差しかえて、1枚のHTMLに結合する。
2. `google.script.run` の受け答えだけを、3年生のサンプルデータを返すものに差しかえる。
3. そのページを実ブラウザ（Chromium）で開き、実際にボタンを押して撮る。

画面のHTML、CSS、JavaScript はリポジトリのものをそのまま使っています。画像の合成や加工はしていません。映っているデータだけがサンプルです。

撮影に使った一式は `.tmp-shots/` に置いていましたが、納品時に削除しています。撮り直すときは、同じ手順を作り直すか、実際にデプロイした環境から撮ってください。1のところは `tools/demo-video/build-preview.mjs` が同じことをしているので、2のサンプルデータを返す部分だけを足せば作り直せます。実環境から撮る場合は、スプレッドシートIDやメールアドレスが映り込まないよう気をつけてください。

撮り直すときはファイル名を変えないでください。本文からの参照が切れます。`--out docs/note/images` に同じ名前で上書きすれば、記事は直さずに済みます。

## 文体について踏襲したもの

- 太字、斜体、表、`→` `／` `：` などの記号を本文で使っていません（この README は入稿用のメモなので表を使っています）。
- 箇条書きは手順のときだけにしています。それ以外は文章です。
- 「デプロイ」「リポジトリ」「API」といった言葉を、先生に伝わる言い方へ置きかえています。
- 絵文字は見出しだけに使っています。
- 教室での出来事を実体験としては書いていません。「こうなればいいと思って作った」「〜だと思います」までにとどめています。効果を言い切っている箇所はありません。
- 見出しは連載の基本形に、目玉の機能ぶんとして「🗓 手が止まるのは『単元』と『学習内容』のところ」と「📊 時数の不安を、年度末まで先回りする」の2本を足しています。追加できる見出しは2本までなので、作った理由の話は新しい節を立てず、🏫 はじめに と 📱 と 📝 まとめ に入れました。
- 本文の字数は約10,300字です。
