# GIGA Standard v5 監査：週案エディタ（SchoolPlan_Editor）

- 実施日: 2026-08-04
- 対象: `claude/rollout-x15qlv`（比較対象は `main` の `2087918`）
- 型: **C+型**（GitHub Pages の PWA シェル `docs/` ＋ GAS ウェブアプリ本体）

## どうやって測ったか

本番（`script.google.com` / `gigayama.github.io`）へはこの作業環境から到達できない。
そこで §7-3 の方法で、GAS が返す画面を手元で組み立てて実ブラウザ（Chromium）で測った。

1. `App.html` の `<?!= include('…'); ?>` を実体に置き換えて1枚のHTMLにする
2. `google.script.run` をダミーに差し替え、**戻り値の見本**を与えて本編まで進める
3. 画面（週案／行事予定／タスク／単元／学級通信／時数／PDF読み込み／設定）を順に開いて走査する
4. 色は「1px 実際に塗って読み返す」方法で測る（§7-2）
5. PWA は `docs/` をそのまま配って、登録・更新・圏外の挙動を測る

**改修前の数字は、`main` の commit を実際にチェックアウトして同じ手順で測った値である**（§7-6）。

### 測るときに引っかかった2点（他のリポジトリでも同じことが起きる）

- **`getImageData` の値を alpha で割り直してはいけない。** 返ってくる値はすでに
  非プリマルチプライ済みで、割ると `rgba(255,255,255,.56)` が `rgb(454,454,454)` になる。
  半透明の面を「実際より明るい」と誤って測る。
- **Playwright の `setOffline(true)` は `127.0.0.1` への通信を止めない。**
  そのまま「圏外でも起動した」と書くと嘘になる。**サーバを止めて**測ること。

---

## A. 法務・配布

| # | 項目 | 前 | 後 | 実測・根拠 |
|---|---|:--:|:--:|---|
| A1 | LICENSE 実ファイル | ❌ | ✅ | MIT / Copyright (c) 2026 GIGAyama |
| A2 | .gitignore | ❌ | ✅ | `node_modules/` `.clasp.json` `.env` ほか |
| A3 | dependabot.yml | ❌ | ✅ | npm / github-actions を monthly。メジャー更新は除外 |
| A4 | README / MANUAL / AUDIT | △ | ✅ | README あり。MANUAL.md・AUDIT.md を新規作成 |
| A5 | CI が pull_request でも動く | ✅ | ✅ | `.github/workflows/quality.yml`（元から対応済み） |

## B. セキュリティ

| # | 項目 | 前 | 後 | 実測・根拠 |
|---|---|:--:|:--:|---|
| B1 | CSP | ❌ | ❌（**未対応**） | 下の「やっていないこと」を参照 |
| B2 | 秘密情報の直書きなし | ✅ | ✅ | 品質ゲートの `SECRET_CANDIDATE` 0件 |
| B3 | OAuth スコープ最小 | ✅ | ✅ | `drive.file`。`auth/drive`（全体）・`mail.google.com` は無し |
| B4 | postMessage の宛先が `*` でない | ❌ | ❌（**据え置き**） | GAS のサンドボックス iframe はオリジンが可変で列挙できない。`quality.config.json` に例外として明記済み。列挙できないものを列挙したふりはしない |
| B5 | サーバー側の多段ガード | ✅ | ✅ | `11_Tenant.gs` ほかで実施（今回は変更していない） |
| B6 | **CDN から取る実行コードが 0** | ❌ | ✅ | 下の P0.5 を参照 |
| B7 | 残る外部資産に SRI と版の固定 | — | — | 実行コードを CDN から取らなくなったため対象なし（Google Fonts は表示のみ） |

## C. 堅牢性

| # | 項目 | 前 | 後 | 実測・根拠 |
|---|---|:--:|:--:|---|
| C1 | LockService + try/finally | ✅ | ✅ | `02_Database.gs` ほか（変更なし） |
| C2 | 自動復旧 | ✅ | ✅ | 列の再生成・整合性チェックあり（変更なし） |
| C3 | pagehide で記録確定 | 未計測 | 未計測 | 週案の保存は明示操作。タブ破棄時の扱いは測っていない |
| C4 | 通信失敗時の明示 | ✅ | ✅ | `_onCommError` で通知 |
| C5 | localStorage.clear() を使っていない | ✅ | ✅ | 0件 |

## D. 表示（Part I §2）— 実ブラウザで全画面を走査

| # | 項目 | 前 | 後 | 実測・根拠 |
|---|---|:--:|:--:|---|
| D1 | viewport に viewport-fit=cover | ❌ | ✅ | `App.html` と `07_WebApp.gs` の**両方**に追加 |
| D2 | 100dvh を使用 | ❌ | ✅ | `#app` と `LoadingModal.html`。`@supports` でフォールバック |
| D3 | safe-area-inset を適用 | ❌ | ✅ | シェルの左右、下部固定要素、`offline.html` |
| D4 | clamp() による fluid type | ❌ | ❌（**据え置き**） | 教員が PC・タブレットで使う画面で、児童が見る「主役の文字」が無い。全面導入は影響が大きいため提案に留める |
| D5 | Canvas の DPR 補正 | — | — | Canvas を使っていない（`getContext('2d')` 0件） |
| D6 | 320px 幅で横スクロールが出ない | ✅ | ✅ | 実測 `scrollWidth 320 / clientWidth 320` |
| D7 | 画像に width/height | ❌ | ✅ | `docs/index.html` の 5枚に付与。150KB 超の画像は 0件 |
| D8 | **コントラスト 4.5:1 以上** | ❌ | ✅ | **ライト 21種→0種／ダーク 27種→0種**（下の表） |
| D9 | **タップ領域 44px 以上** | ❌ | ✅ | **38種→0種**（チェックボックスは囲みラベル 44px で確保。実測で確認） |
| D10 | prefers-reduced-motion（`.01ms`） | ✅ | ✅ | `0.001ms`。`0` ではない |
| D11 | forced-colors 対応 | ❌ | ✅ | ボタン・ナビ・選択中セルに枠線 |
| D12 | 提示モード | — | — | 教員が個人で使う校務アプリで、一斉授業では使わない |
| D13 | 印刷CSS | ✅ | ✅ | 週案・学級通信は `@page` を持つ。あとから足した固定要素にも `no-print` を付けた |
| D14 | 拡大を禁止していない | ✅ | ✅ | `user-scalable=no` / `maximum-scale` は 0件 |

### D8 の内訳（ライトモード・色の組み合わせ単位）

| 前の比 | どこ | 直し方 | 後 |
|---:|---|---|---:|
| 1.55 | 「データ保全・復元」の見出し（濃紺の帯に濃いグレーの字） | 帯の色を継がせる（`color: inherit`）＋帯側に白を指定 | 8.6 |
| 1.87 | ヘッダーのアプリのマーク（色指定が無く本文色を継いでいた） | 白を明示 | 8.6 |
| 2.05 | SweetAlert2 の情報アイコン（ライブラリ既定色） | `#087990` に置き換え | 5.0 |
| 2.39〜2.64 | 案内文・空状態の説明（`--text-hint: #9aa0a6`） | `#686c70` へ1段濃く | 4.8〜5.3 |
| 2.88〜3.21 | 濃い帯（緑・橙）の上の見出し | 帯を1段濃くし、文字を白に | 5.0〜6.1 |
| 3.01 | ヘッダーの「第◯週 表示中」 | 半透明をやめる | 5.3 |
| 3.06〜3.92 | 緑・赤・青・橙の**面用の色を文字に使っていた**箇所 | 文字用トークン（`--primary-text` ほか）を新設して置き換え | 4.5〜6.0 |
| 3.17〜3.45 | ヘッダーのナビ（半透明の白／白を重ねた選択中の面） | 帯を `#1665cf` に1段濃く、選択中は暗くして示す | 5.5〜7.7 |

**一括置換のあとに測り直している。** `color: var(--primary)` を 63か所まとめて置き換えたため、
濃い面の上の文字が逆に読めなくなっていないかを、全画面の再走査で確認した（0件）。

### ダークモードで見つかったこと（改修前から壊れていた）

| 前の比 | 何が起きていたか |
|---:|---|
| 1.07 | **Bootstrap の `.btn` 既定色（#212529）** が暗い面の上に乗り、輪郭ボタンの文字がほぼ見えない（55か所） |
| 1.05 | `.text-muted`（Bootstrap 既定 `rgba(33,37,41,.75)`）も同様 |
| 1.06〜1.78 | 明るい面を固定で持つ部品（学級通信のパレット）に、暗いテーマ用の明るい文字が乗る |
| 2.63 | 日曜列の赤 `#c62828` を決め打ちしていた |

アプリ固有の配色を疑う前に、**フレームワークの既定色を疑う**という §2-8 のとおりだった。

## E. PWA（Part I §3）— `docs/` を配って実測

| # | 項目 | 前 | 後 | 実測・根拠 |
|---|---|:--:|:--:|---|
| E1 | manifest の id/scope/start_url | ✅ | ✅ | `/SchoolPlan_Editor/` の絶対パス（元から正しい） |
| E2 | アイコン4種 + 透明を含まない apple-touch-icon | ✅ | ✅ | 画素で確認：apple-touch-icon の**透明 0.00%** |
| E3 | beforeinstallprompt を head 最上部の外部ファイルで捕捉 | ❌ | ✅ | `docs/install-hook.js` を新設し `<head>` 先頭で同期読み込み |
| E4 | インストールボタン（案内できるときだけ） | ✅ | ✅ | Chrome は合図が来たときだけ、iOS は手順ガイド |
| E5 | 自アプリ接頭辞のキャッシュのみ削除 | ✅ | ✅ | 別アプリ名のキャッシュを2つ置いて版を上げ、**残っていることを確認** |
| E6 | sw.js が localStorage に触れていない | ✅ | ✅ | 0件 |
| E7 | 更新通知（押すまで切り替わらない） | ❌ | ✅ | 版を上げて `update()` → **3秒放置で `waiting` のまま**・`controllerchange` 発火なし。押すと切り替わる |
| E8 | 初回訪問で勝手にリロードしない | ✅ | ✅ | まっさらな状態で**画面遷移 1回** |
| E9 | Service Worker が実際に登録されている | ✅ | ✅ | `getRegistration()` が返る。`readyState` の分岐も追加 |
| E10 | offline.html | ❌ | ✅ | 前：本体キャッシュが無いと `net::ERR_FAILED`（ブラウザ既定のエラー画面）／後：`offline.html` が出る |
| E11 | APP_VERSION を更新した | — | ✅ | `schoolplan-editor-shell-v2` → `v3` |
| E12 | maskable のセーフゾーン外の中身 0.2% 以下 | ✅ | ✅ | 画素で確認：**0.02%**（192・512とも）。下地は四隅まで届いている |
| E13 | iOS の「ホーム画面に追加」手順 | ✅ | ✅ | シェル内にガイドあり。MANUAL.md にも記載 |

## F. アクセシビリティ・性能

| # | 項目 | 前 | 後 | 実測・根拠 |
|---|---|:--:|:--:|---|
| F1 | alt / aria-label / aria-live | △ | △ | 追加した要素には付与。全体の網羅は**未計測** |
| F2 | モーダルに role="dialog"・Esc で閉じる | 未計測 | 未計測 | SweetAlert2 側は対応。アプリ独自モーダルは未計測 |
| F3 | キーボードのみで全機能に到達 | 未計測 | 未計測 | Tab 順の通し確認はしていない |
| F4 | rt の色を決め打ちしていない | — | — | ルビを使っていない（教員向けのため） |
| F5 | 初回JS 300KB以下 | ❌ | ❌（**未達**） | 下の「積み残し」を参照 |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | ✅ | 品質ゲートの警告 0件（`App_Css.html` 4,918行） |

## G. 学習ログ

対象外。児童の学習記録を持たない（教員の校務アプリ）。

---

## P0.5：CDN から取る実行コードを 0 にした（今回いちばん効く修正）

改修前は、次の3つを外部CDNから読んでいた。**学校のフィルタリングで塞がれると何が起きるか**を
実際に遮断した状態で測った。

| 読んでいたもの | 塞がれると | 実測 |
|---|---|---|
| Bootstrap 5.3.3 CSS（jsdelivr） | レイアウトが崩れる | ボタン・グリッドの体裁が崩れる |
| Bootstrap Icons（jsdelivr） | アイコンが豆腐になる | — |
| **SweetAlert2 @11（版が浮いたまま）** | 確認ダイアログが `ReferenceError` で止まる | `Swal` が `undefined` |
| **Material Symbols（Google Fonts）** | **アイコンが英単語で表示される** | 「calendar_month」「task_alt」がそのまま画面に出る |

**Material Symbols は「見た目だけの依存」ではない。** §2-7 は Web フォントを
「字の形が変わるだけ」として CDN を許しているが、**アイコンフォントは別である。**
合字が効かないと、絵ではなく `radio_button_unchecked` という英単語が並び、
レイアウトも崩れる。先生には「壊れている」としか見えない。

### 直した結果

すべて `tools/build-vendor.mjs` でリポジトリに焼き込み、GAS が配る `.html` にした。

| ファイル | 中身 | 大きさ |
|---|---|---:|
| `Vendor_Bootstrap.html` | Bootstrap 5.3.3 の CSS | 227.5 KB |
| `Vendor_Icons.html` | Material Symbols（**使用中の150個だけ**）＋ Bootstrap Icons（8個だけ） | 23.3 KB |
| `Vendor_Sweetalert.html` | SweetAlert2 11.14.5 の CSS と JS | 69.3 KB |

Material Symbols のフォントは **488.7 KB → 14.9 KB**（可変軸を固定し、使うグリフだけに絞った）。

**CDN を全部遮断した状態で全画面を開いて確認した結果：英単語のまま出るアイコン 0個、JS エラー 0件。**

### アイコンのサブセットで踏んだ穴（他リポジトリでも踏む）

- `--text=` にアイコン名を並べる素朴なやり方では小さくならない（**347.6 KB**）。
  合字の閉包で、同じ文字を使う何千個ものアイコンが道連れで残る。
  `--glyphs=` ＋ `--no-layout-closure` にすると **14.9 KB**。
- **アイコン名 = グリフ名とは限らない。** `auto_fix_high` は `auto_fix` に置換される。
- **`@material-symbols` の名前一覧（index.d.ts）で「既知の名前だけ」に絞ってはいけない。**
  現行名の一覧なので、実際に使われている別名（`auto_awesome` など）が落ちる。
- **フォントの表を読むだけでは確かめられない。** 表の上では合字が引けているのに、
  ブラウザでは英単語が出た。`npm run verify:icons` で**実ブラウザに描かせて幅を測る**。
  （絵なら幅は 1em、英単語なら数倍になる）

---

## 積み残し（やっていないこと・測っていないこと）

**「未計測」は ✅ ではない。**

| 項目 | なぜやっていないか |
|---|---|
| **CSP（B1）** | 本体が GAS のサンドボックス iframe 内で動き、`onclick=` によるイベント配線が全画面にわたって多数ある。入れると**確実に壊れる**うえ、本番で動作確認ができない（`script.google.com` へ到達できない）。§2-13 の「入れたら必ず動かして確かめる」を満たせないため、投入しない |
| **初回JS 300KB（F5）** | 本体の JS だけで約 600KB ある。減らすにはファイル分割と読み込みの遅延化が要り、**アーキテクチャの変更**にあたる。§P3 のとおり分割案の合意が先 |
| **fluid type（D4）** | 教員向け画面で「主役の文字」が無く、全面 clamp() 化は見た目の変更が大きい |
| **キーボード操作・モーダルの a11y（F2/F3）** | 走査ではなく人が触って確かめる必要がある。今回は未実施 |
| **`pagehide` での確定保存（C3）** | Chromebook のタブ破棄時にどうなるかを測っていない |
| **本番での動作確認** | `script.google.com` / `gigayama.github.io` へ到達できない。**GAS 本体（`.gs` / `.html`）の変更は、テストデプロイで確かめてからマージすること** |
| **印刷の実出力** | 印刷プレビューの目視確認をしていない（CSS の有無のみ確認） |

---

## この監査を再現する方法

```bash
npm install
npm run check          # 静的な検査（GIGA v5 Part I を含む）
npm test               # 検査自身のテスト（わざと壊して拾えることを確認）
npm run build:vendor   # 外部ライブラリの焼き込みを作り直す
npm run verify:icons   # アイコンが絵として描かれるかを実ブラウザで確認
```

コントラスト・タップ領域・PWA の挙動は、`App.html` を手元で貼り合わせて
Chromium で走査して測っている（手順は本文冒頭のとおり）。
