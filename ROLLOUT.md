# ロールアウト記録（SchoolPlan_Editor）

GIGA Standard v5 `/rollout` の作業記録。
**他のリポジトリにも効く知見**を中心に残す。数字はすべて実測値。

- 実施: 2026-08-04
- ブランチ: `claude/rollout-x15qlv`
- 比較対象: `main` の `2087918`（実際にチェックアウトしてビルドし、同じ手順で測った）

## やったこと

| フェーズ | 内容 | 実測（前 → 後） |
|---|---|---|
| P0 | LICENSE / .gitignore / dependabot.yml を作成 | 3件欠け → 0件 |
| P0.5 | CDN から取る実行コードを 0 に（Bootstrap・SweetAlert2・Material Symbols を焼き込み） | 4本 → 0本。遮断時のアイコン化け 4種 → 0種 |
| P1 表示 | viewport-fit / dvh / safe-area / コントラスト / タップ44px / forced-colors | コントラスト ライト21種→0種・ダーク27種→0種、タップ38種→0種 |
| P1 PWA | install の skipWaiting 撤去・更新通知・offline.html・登録の readyState 分岐・install-hook.js | 更新が黙って入れ替わる → 押すまで待機。圏外の本体欠けが `ERR_FAILED` → `offline.html` |
| P4 | 品質ゲートに Part I の検査を追加（`scripts/lib/giga-v5-checks.mjs`）と、その自己テスト24本 | 検査 0件（わざと壊して拾えることを確認済み） |

詳細と「測っていないもの」は `AUDIT.md` を参照。

---

## 他のリポジトリにも効く知見

### 1. アイコンフォントは「見た目だけの依存」ではない 🔴

v5 §2-7 は Google Fonts を「字の形が変わるだけなので CDN でよい」としているが、
**Material Symbols（合字で描くアイコンフォント）には当てはまらない。**

塞がれると、絵ではなく `calendar_month` `radio_button_unchecked` という
**英単語がそのまま画面に並ぶ**。文字数ぶん幅を取るのでレイアウトも崩れる。

```bash
# 同じ形が他に何本あるか
grep -rln "fonts.googleapis.com/css2?family=Material+Symbols" $(git ls-files '*.html' '*.gs')
grep -rln "material-icons" $(git ls-files '*.html')
```

**直し方は §6 と同じ「先に作っておく」。** `tools/build-vendor.mjs` がそのまま流用できる。
実測：488.7 KB のフォント → **使用中150個で 14.9 KB**。

### 2. アイコンフォントのサブセットは、素朴にやると効かない 🔴

| やり方 | 大きさ | 結果 |
|---|---:|---|
| `pyftsubset --text="icon_a icon_b …"` | **347.6 KB** | 合字の閉包で、同じ文字を使う数千個が道連れで残る |
| `--glyphs=<グリフ名> --text=<英小文字> --no-layout-closure` | **14.9 KB** | ◯ |

さらに3つ落とし穴がある。

- **アイコン名 = グリフ名とは限らない。** `auto_fix_high` は `auto_fix` に置換される。
  合字表（GSUB）を読んで置換先を引く（`tools/ms-ligatures.py`）。
- **`@material-symbols` の名前一覧で「既知の名前だけ」に絞ると、別名が落ちる。**
  `auto_awesome` `magic_button` が一覧に無く、3個が英単語のまま残った。
  タグの中身は定義上アイコン名なので、そのまま採ること。
- **フォントの表を読むだけでは確かめられない。** 表の上では引けているのに
  ブラウザでは英単語が出た。**実ブラウザに描かせて幅を測る**のが唯一確実
  （絵なら 1em、英単語なら数倍）。`npm run verify:icons` がそれ。

### 3. コントラストの実測コードにバグがある（v5 §7-2 のひな形） 🔴

標準に載っているひな形は `getImageData` の値を alpha で割り直しているが、
**getImageData はすでに非プリマルチプライ済みの値を返す。**

```js
// ❌ ひな形のまま：rgba(255,255,255,.56) が rgb(454,454,454) になる
return a === 0 ? [0,0,0,0] : [d[0]/a, d[1]/a, d[2]/a, a];
// ✅
return [d[0], d[1], d[2], d[3] / 255];
```

半透明の面を「実際より明るい」と誤って測るため、**半透明のヘッダー・オーバーレイを
使っているリポジトリでは数字が全部ずれる。**

あわせて、**背景は「根から順に重ねる」こと。** 各レイヤーを別々に白へ重ねると、
青い帯の上の半透明の白が「白の上の白（比 1.0）」という誤報になる。

### 4. Playwright の `setOffline(true)` は 127.0.0.1 を止めない 🔴

ローカルサーバで PWA を測ると、圏外にしたつもりでも**普通に通信できてしまう。**
「圏外でも起動しました」と書いても、それは何も測っていない。
**サーバのプロセスを止めて**測ること。

このリポジトリでは、止めて測った結果として
「本体キャッシュが無いと `net::ERR_FAILED`（＝ offline.html が出ていない）」が見つかった。

### 5. ダークモードを持つリポジトリは、ライブラリ既定色で必ず壊れている 🟡

Bootstrap の `.btn` は既定で `#212529`（濃いグレー）の文字。
暗い面の上に乗ると **比 1.07**。このリポジトリでは輪郭ボタン55か所が実質見えていなかった。
`.text-muted`（`rgba(33,37,41,.75)`）も同じ。

**ライトだけ測って「0件」と書かない。** ダークも走査すること。

```bash
grep -rln "prefers-color-scheme: dark" $(git ls-files '*.html' '*.css')
```

### 6. 「色を決め打ちしない、まとめて継がせる」は ruby 以外にも効く 🟡

v5 §4 は `rt`（ふりがな）の話として書かれているが、同じ形が見出しでも起きていた。

```css
/* ❌ 帯の色が濃くても、見出しは常に濃いグレー（比 1.55） */
.integration-title { color: var(--text-main); }
/* ✅ 帯の色を継ぐ。帯側で色を決める */
.integration-title { color: inherit; }
```

**1か所ずつ潰さない。** このリポジトリでは濃い帯が3か所あり、
1つだけ手当てしても残り2つが漏れる形だった。

### 7. 品質ゲートは「わざと壊して」確かめる（v5 §P4 のとおり） 🟡

`scripts/lib/giga-v5-checks.mjs` を書いて自己テスト24本を通したところ、
**検査そのものの不具合が3件**見つかった。

| 不具合 | 中身 |
|---|---|
| `GIGA_VIEWPORT_FIT` が何も見ていなかった | `content="…"` の `=` を正規表現に入れ忘れ、HTML 側が1件も引っかかっていなかった。**「0件」は動いている証拠にならない** |
| `GIGA_VIEWPORT_100VH` の見逃し | 「近くに dvh があるか」で判定すると、`@supports` フォールバックの隣に書いた別の `100vh` を見逃す。**ブロックごと外してから探す** |
| `GIGA_INSTALL_HOOK_INLINE` の誤検知 | 「Safari には beforeinstallprompt が無い」という**注意書き**に反応。判定前にコメントを落とす |

3件目は v5 が `SW_LOCALSTORAGE` について書いている誤検知とまったく同じ形だった。
**コメントを落としてから判定する**のは、この種の検査の基本動作として入れておくとよい。

---

## 人間に確認してほしいこと

1. **GAS 本体（`.gs` / `.html`）の変更は、テストデプロイで動作を確かめてからマージする。**
   この作業環境から `script.google.com` へ到達できないため、本番相当の確認ができていない。
   特に見てほしいのは次の3点。
   - 画面のアイコンがすべて絵として出るか（英単語が出ていないか）
   - 確認ダイアログ（SweetAlert2）が開くか
   - ボタンが押せるか（タップ領域を疑似要素で広げたため、隣のボタンと重なっていないか）
2. `School_plan_note` と `SchoolPlan_Editor` のどちらを正本にするか（v5 Part V の未決事項）。
   同一オリジンのキャッシュ接頭辞は分けてあるので、当面は共存できる。
3. CSP は入れていない。理由は `AUDIT.md`。入れるなら `onclick=` の配線を
   `addEventListener` に置き換える作業が先に要る（全画面規模）。

## 次にやるなら

- 初回 JS 600KB の削減（分割と遅延読み込み。**アーキテクチャの変更なので合意が先**）
- キーボード操作だけで全機能に到達できるかの通し確認
- 印刷プレビューの目視確認（A4縦・背景グラフィック）
